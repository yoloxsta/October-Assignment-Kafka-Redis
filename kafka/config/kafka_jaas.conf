// Client JAAS Configuration
// Used by backend to connect to Kafka with username/password

KafkaClient {
    org.apache.kafka.common.security.plain.PlainLoginModule required
    username="backend"
    password="backend-secret";
};
